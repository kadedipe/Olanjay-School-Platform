import Link from "next/link";
type Action = string | { label: string; href: string };
type DashboardProps = { eyebrow: string; title: string; description: string; metrics: Array<[string,string,string]>; actions: Action[] };
export function RoleDashboard({ eyebrow,title,description,metrics,actions }: DashboardProps) { return <><header><div><p className="eyebrow">{eyebrow}</p><h1>{title}</h1><p>{description}</p></div></header><section className="metrics">{metrics.map(([label,value,note])=><article className="metric" key={label}><p>{label}</p><strong>{value}</strong><small>{note}</small></article>)}</section><section className="panel"><div className="panelHead"><div><p className="eyebrow">YOUR WORKSPACE</p><h2>Quick actions</h2></div></div><div className="actionGrid">{actions.map((action) => typeof action === "string" ? <button key={action}>{action}<span>→</span></button> : <Link key={action.href} href={action.href}>{action.label}<span>→</span></Link>)}</div></section></>; }
