"use client";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
import { requestJson } from "@/lib/client-api";
export function UserStatusForm({userId,status,isSelf}:{userId:string;status:"ACTIVE"|"SUSPENDED"|"ARCHIVED";isSelf:boolean}){const router=useRouter(),[pending,setPending]=useState(false),[error,setError]=useState("");async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setPending(true);setError("");try{await requestJson(`/api/users/${userId}/status`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({status:String(new FormData(event.currentTarget).get("status"))})});router.refresh();}catch(cause){setError(cause instanceof Error?cause.message:"Account status could not be changed.");}finally{setPending(false);}}return <form className="inlineStatusForm" onSubmit={submit}><select name="status" defaultValue={status} disabled={pending||isSelf} aria-label="Account status"><option value="ACTIVE">Active</option><option value="SUSPENDED">Suspended</option><option value="ARCHIVED">Archived</option></select><button className="secondary" disabled={pending||isSelf}>{pending?"Saving…":"Update"}</button>{isSelf&&<small>Current account</small>}{error&&<small className="formError">{error}</small>}</form>}
