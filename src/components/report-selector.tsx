"use client";import{FormEvent,useState}from"react";import{useRouter}from"next/navigation";type O={id:string;label:string};
export function ReportSelector({students,terms}:{students:O[];terms:O[]}){const router=useRouter(),[studentId,setStudentId]=useState(students.length===1?students[0].id:""),[termId,setTermId]=useState("");function submit(event:FormEvent){event.preventDefault();if(studentId&&termId)router.push(`/dashboard/reports/${studentId}/${termId}`);}return <form className="crudForm compactForm" onSubmit={submit}><div className="formGrid"><label>Student<select required value={studentId} onChange={event=>setStudentId(event.target.value)}><option value="" disabled>Select student</option>{students.map(student=><option key={student.id} value={student.id}>{student.label}</option>)}</select></label><label>Academic term<select required value={termId} onChange={event=>setTermId(event.target.value)}><option value="" disabled>Select term</option>{terms.map(term=><option key={term.id} value={term.id}>{term.label}</option>)}</select></label></div><div className="formActions"><button className="primary" disabled={!studentId||!termId}>Open report card</button></div></form>;}
