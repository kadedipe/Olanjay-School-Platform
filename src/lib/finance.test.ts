import{describe,expect,it}from"vitest";import{InvoiceStatus,PaymentStatus}from"@prisma/client";import{invoiceBalance,resolvedInvoiceStatus}from"./finance";
describe("finance utilities",()=>{it("counts only successful payments",()=>expect(invoiceBalance(1000,[{amount:400,status:PaymentStatus.SUCCEEDED},{amount:500,status:PaymentStatus.FAILED}])).toEqual({paid:400,balance:600}));it("resolves invoice states",()=>{expect(resolvedInvoiceStatus(InvoiceStatus.ISSUED,1000,[{amount:1000,status:PaymentStatus.SUCCEEDED}],new Date("2026-12-01"),new Date("2026-09-12"))).toBe(InvoiceStatus.PAID);expect(resolvedInvoiceStatus(InvoiceStatus.ISSUED,1000,[{amount:250,status:PaymentStatus.SUCCEEDED}],new Date("2026-12-01"),new Date("2026-09-12"))).toBe(InvoiceStatus.PARTIALLY_PAID);expect(resolvedInvoiceStatus(InvoiceStatus.ISSUED,1000,[],new Date("2026-01-01"),new Date("2026-09-12"))).toBe(InvoiceStatus.OVERDUE);});});
