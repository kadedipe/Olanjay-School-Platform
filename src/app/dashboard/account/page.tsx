import{Role}from"@prisma/client";import{requireUser}from"@/lib/authorization";import{prisma}from"@/lib/prisma";import{ProfileForm}from"@/components/profile-form";import{PasswordForm}from"@/components/password-form";
export default async function AccountPage(){const session=await requireUser([Role.ADMIN,Role.TEACHER,Role.STUDENT,Role.GUARDIAN]),user=await prisma.user.findUniqueOrThrow({where:{id:session.id},include:{student:true,teacher:true,guardian:true}});const identifier=user.student?.admissionNumber??user.teacher?.employeeNumber??"Not assigned";return <main><header><div><p className="eyebrow">ACCOUNT SETTINGS</p><h1>Profile and security</h1><p>Maintain your contact details and protect access to your school account.</p></div></header><section className="accountGrid"><div className="panel"><div className="panelHead"><div><p className="eyebrow">PROFILE</p><h2>Personal details</h2></div></div><div className="accountIdentity"><div><span>Email</span><strong>{user.email}</strong></div><div><span>Role</span><strong>{user.role.toLowerCase()}</strong></div><div><span>{user.role===Role.STUDENT?"Admission number":user.role===Role.TEACHER?"Employee number":"Account identifier"}</span><strong>{identifier}</strong></div><div><span>Last sign-in</span><strong>{user.lastLoginAt?.toLocaleString("en-UG")??"Not recorded"}</strong></div></div><ProfileForm firstName={user.firstName} lastName={user.lastName} phone={user.phone??""}/></div><div className="panel"><div className="panelHead"><div><p className="eyebrow">SECURITY</p><h2>Change password</h2></div></div><PasswordForm/></div></section></main>;}
