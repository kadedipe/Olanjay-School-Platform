import { Role } from "@prisma/client";
import { requireUser } from "@/lib/authorization";
import { RoleDashboard } from "@/components/role-dashboard";
import { InvitationForm } from "@/components/invitation-form";
import { prisma } from "@/lib/prisma";
export default async function AdminDashboard(){ await requireUser([Role.ADMIN]); const [activeUsers,pendingInvitations,students,courses] = await Promise.all([prisma.user.count({where:{status:"ACTIVE"}}),prisma.invitation.count({where:{acceptedAt:null,revokedAt:null,expiresAt:{gt:new Date()}}}),prisma.student.count({where:{user:{status:{not:"ARCHIVED"}}}}),prisma.course.count({where:{isActive:true}})]); return <main><RoleDashboard eyebrow="ADMINISTRATION" title="School operations" description="Manage access, academics, finance, and institutional performance." metrics={[["Active users",String(activeUsers),"Connected to live records"],["Pending invitations",String(pendingInvitations),"Awaiting acceptance"],["Students",String(students),"Active academic records"],["Courses",String(courses),"Active programmes"]]} actions={[{label:"Manage enrollments",href:"/dashboard/enrollments"},{label:"Assign teachers",href:"/dashboard/teaching"},{label:"Build timetable",href:"/dashboard/timetable"},{label:"Review attendance",href:"/dashboard/attendance"}]}/><section className="panel dashboardSection"><div className="panelHead"><div><p className="eyebrow">ACCESS MANAGEMENT</p><h2>Invite a user</h2></div></div><InvitationForm/></section></main>; }
