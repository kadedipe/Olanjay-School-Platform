import { Role } from "@prisma/client"; import { requireUser } from "@/lib/authorization"; import { RoleDashboard } from "@/components/role-dashboard";
export default async function GuardianDashboard(){ await requireUser([Role.GUARDIAN]); return <main><RoleDashboard eyebrow="GUARDIAN PORTAL" title="Student overview" description="Follow linked students’ attendance, academic progress, and fee status." metrics={[["Linked students","—","Verified relationships"],["Attendance alerts","—","Current term"],["New results","—","Published grades"],["Fee balance","—","All linked students"]]} actions={["Choose student","View attendance","View results","View invoices"]}/></main>; }
