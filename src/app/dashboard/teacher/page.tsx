import { Role } from "@prisma/client"; import { requireUser } from "@/lib/authorization"; import { RoleDashboard } from "@/components/role-dashboard";
export default async function TeacherDashboard(){ await requireUser([Role.TEACHER]); return <main><RoleDashboard eyebrow="TEACHING" title="Your classes" description="Manage assigned classes, attendance, assessments, and student progress." metrics={[["Classes today","—","Current timetable"],["Students","—","Assigned courses"],["Attendance","—","Today"],["Results pending","—","Awaiting entry"]]} actions={["Mark attendance","View timetable","Enter results","View students"]}/></main>; }
